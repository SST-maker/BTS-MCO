import { updateTeacherPassword } from './supabase-client.js'; import { $,toast } from './common.js';
const root=$('#root');root.innerHTML=`<div class="auth-shell single"><section class="auth-panel"><div class="auth-card"><div class="mini-logo"><img src="./assets/icons/icon-96.png"><div><b>MCO Quiz Arena</b><small>Sécurité professeur</small></div></div><h2>Nouveau mot de passe</h2><p class="muted">Choisis au minimum 10 caractères.</p><div class="field"><label>Nouveau mot de passe</label><input id="password" type="password" autocomplete="new-password"></div><button id="save" class="btn primary premium-cta">Enregistrer</button></div></section></div>`;$('#save').onclick=async()=>{const p=$('#password').value;if(p.length<10)return toast('10 caractères minimum','bad');try{await updateTeacherPassword(p);toast('Mot de passe mis à jour');setTimeout(()=>location.href='./teacher.html',700)}catch(e){toast(e.message,'bad')}};
