import { rpc, isConfigured } from './supabase-client.js';
import { $, qs, playerStore, toast, esc, configWarning } from './common.js';
const root=$('#root');let code=(qs('code')||'').toUpperCase();
function view(preview=null){root.innerHTML=`<div class="join-wrap"><section class="join-brand"><img src="./assets/icons/icon-192.png"><h1>Rejoins le Quiz Arena !</h1><p>Entre le code affiché au tableau puis choisis ton pseudo. Tu répondras directement depuis ton téléphone.</p><div class="eyebrow" style="background:rgba(255,255,255,.12);color:#fff">NCR SOLUTIONS BTS MCO</div></section><section class="join-panel"><div class="join-card"><h2>Rejoindre une partie</h2>${!isConfigured?configWarning():''}<label class="muted">Code de partie</label><input id="code" maxlength="5" value="${esc(code)}" placeholder="7B3K2" style="text-transform:uppercase;letter-spacing:.18em;font-weight:900;text-align:center"><div id="preview">${preview?`<div class="notice"><b>${esc(preview.title)}</b><br>${esc(preview.mode)} • ${preview.players} connecté(s)</div>`:''}</div><label class="muted">Ton pseudo</label><input id="name" maxlength="24" placeholder="TeamNCR 🚀"><label class="muted">Équipe (optionnel)</label><input id="team" maxlength="24" placeholder="Équipe A"><button class="btn primary" id="join" style="width:100%">Rejoindre</button><div class="toolbar"><a class="btn soft" href="./index.html" style="width:100%">Retour à l’accueil</a></div></div></section></div>`;wire()}
async function preview(){if(!isConfigured||code.length!==5)return;try{const p=await rpc('mco_session_preview',{p_code:code});const el=$('#preview');if(el)el.innerHTML=`<div class="notice"><b>${esc(p.title)}</b><br>${esc(p.mode)} • ${p.players} connecté(s)</div>`}catch{}}
function wire(){const codeEl=$('#code');codeEl.oninput=()=>{code=codeEl.value.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,5);codeEl.value=code};codeEl.onblur=preview;$('#join').onclick=join}
async function join(){if(!isConfigured){toast('Configure d’abord Supabase dans config.js','bad');return}const name=$('#name').value.trim(),team=$('#team').value.trim();code=$('#code').value.trim().toUpperCase();if(code.length!==5||!name){toast('Code et pseudo requis','bad');return}try{$('#join').disabled=true;const data=await rpc('mco_join_session',{p_code:code,p_name:name,p_team:team||null});playerStore(code,{playerId:data.playerId,playerToken:data.playerToken,name});location.href=`./play.html?code=${encodeURIComponent(code)}`}catch(e){toast(e.message,'bad');$('#join').disabled=false}}
view();preview();
