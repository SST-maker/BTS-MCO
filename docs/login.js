import { signInTeacher, requestPasswordReset, getSession, teacherMe, signOutTeacher, isConfigured } from './supabase-client.js';
import { $, qs, esc, toast, configWarning } from './common.js';
const root=$('#root');
const err=qs('error');
function render(){root.innerHTML=`<div class="auth-shell"><section class="auth-brand"><div class="auth-brand-inner"><img src="./assets/icons/icon-512.png" class="auth-icon"><div class="eyebrow light">NCR SOLUTIONS • BTS MCO</div><h1>Ton espace professeur.<br><span>Simple, rapide, premium.</span></h1><p>Crée tes parties, pilote le direct, analyse les résultats et garde la banque de questions protégée.</p><div class="auth-points"><span>✓ Accès sécurisé</span><span>✓ 1 620 questions</span><span>✓ Classement live</span></div></div></section><section class="auth-panel"><div class="auth-card"><a class="auth-back" href="./index.html">← Accueil</a><div class="mini-logo"><img src="./assets/icons/icon-96.png"><div><b>MCO Quiz Arena</b><small>Espace professeur</small></div></div><h2>Connexion</h2><p class="muted">Réservé aux comptes professeur autorisés.</p>${!isConfigured?configWarning():''}${err?`<div class="notice danger">${esc(err)}</div>`:''}<div class="field"><label>Adresse e-mail</label><input id="email" type="email" autocomplete="email" placeholder="prof@exemple.fr"></div><div class="field"><label>Mot de passe</label><div class="password-wrap"><input id="password" type="password" autocomplete="current-password" placeholder="••••••••"><button id="togglePwd" type="button">Afficher</button></div></div><button class="btn primary premium-cta" id="login">Se connecter <span>→</span></button><button class="link-btn" id="forgot">Mot de passe oublié ?</button><div class="auth-security">🔒 Aucun compte élève requis. Les élèves rejoignent uniquement avec le code de partie.</div></div></section></div>`;wire()}
function wire(){const p=$('#password');$('#togglePwd').onclick=()=>{p.type=p.type==='password'?'text':'password';$('#togglePwd').textContent=p.type==='password'?'Afficher':'Masquer'};$('#login').onclick=login;$('#password').onkeydown=e=>{if(e.key==='Enter')login()};$('#forgot').onclick=forgot}
async function login(){if(!isConfigured)return toast('Configure Supabase dans config.js','bad');const email=$('#email').value.trim(),pwd=$('#password').value;if(!email||!pwd)return toast('E-mail et mot de passe requis','bad');try{$('#login').disabled=true;await signInTeacher(email,pwd);await teacherMe();const next=qs('next')||'teacher.html';location.href=`./${next.replace(/^\.\//,'')}`}catch(e){await signOutTeacher();toast(e.message,'bad');$('#login').disabled=false}}
async function forgot(){const email=$('#email').value.trim();if(!email)return toast('Saisis ton e-mail professeur','bad');try{await requestPasswordReset(email);toast('Lien de réinitialisation envoyé')}catch(e){toast(e.message,'bad')}}
(async()=>{if(isConfigured){const s=await getSession();if(s){try{await teacherMe();location.href='./teacher.html';return}catch{await signOutTeacher()}}}render()})();
