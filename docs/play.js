import { rpc, isConfigured, liveChannel } from './supabase-client.js';
import { $, qs, playerLoad, esc, toast, uiIcon } from './common.js';
import { avatarMarkup } from './avatar.js';
const root=$('#root');const code=(qs('code')||'').toUpperCase();const auth=playerLoad(code);let session=null,channel=null,poller=null,timer=null,last='',questionShownAt=Date.now();
function shell(content){root.innerHTML=content}
function lobby(){const av=session.player?.avatar||auth?.avatar;shell(`<div class="player-live-shell"><header class="player-live-top"><a href="./index.html" class="player-live-brand"><img src="./assets/icons/icon-64.png" alt=""><span><b>MCO Quiz Arena</b><small>LIVE • ${code}</small></span></a><div class="player-live-chip">${av?avatarMarkup(av,'avatar-svg avatar-game-svg',`Avatar de ${session.player?.name||''}`):''}<span>${esc(session.player?.name||'Élève')}</span></div></header><main class="player-lobby-v7"><div class="player-lobby-orbit"></div>${av?`<div class="live-avatar-hero">${avatarMarkup(av,'avatar-svg live-avatar-svg',`Avatar de ${session.player?.name||''}`)}</div>`:'<img src="./assets/icons/icon-192.png" alt="">'}<div class="section-kicker">TU ES CONNECTÉ</div><h1>Prêt, ${esc(session.player?.name||'')} ?</h1><p>Le professeur va lancer la première question. Garde cet écran ouvert.</p><div class="player-lobby-stats"><span>${uiIcon('users')}<b>${session.players.length}</b><small>connecté${session.players.length>1?'s':''}</small></span><span>${uiIcon('live')}<b>${code}</b><small>code de session</small></span></div><div class="player-wait-line"><i></i><span>En attente du lancement…</span></div></main></div>`) }
function startTimer(){clearInterval(timer);let left=session.secondsRemaining??session.settings.timerSeconds;const el=$('#playerTimer');timer=setInterval(()=>{left--;if(el)el.textContent=Math.max(0,left)+'s';if(left<=0){clearInterval(timer);document.querySelectorAll('[data-answer]').forEach(b=>b.disabled=true)}},1000)}
function question(){questionShownAt=Date.now();const q=session.question;const answered=session.player?.answer!=null;shell(`<div class="player-live-shell"><header class="player-live-top"><div class="player-live-brand"><img src="./assets/icons/icon-64.png" alt=""><span><b>${esc(q.subject)} • ${esc(q.chapter)}</b><small>QUESTION ${session.currentIndex+1}/${session.totalQuestions}</small></span></div><div class="player-question-meta">${session.player?.avatar?avatarMarkup(session.player.avatar,'avatar-svg avatar-game-svg',`Avatar de ${session.player?.name||''}`):''}<span class="player-score-chip">${session.player?.score||0} pts</span><div class="player-timer" id="playerTimer">${session.secondsRemaining??session.settings.timerSeconds}s</div></div></header><main class="player-question-v7"><div class="player-question-progress"><i style="width:${(session.currentIndex+1)/session.totalQuestions*100}%"></i></div><div class="player-question-index">LEÇON ${esc(q.lesson)} • QUESTION ${session.currentIndex+1}</div><h1>${esc(q.prompt)}</h1><div class="answer-grid player-answer-grid">${q.choices.map((a,i)=>`<button class="answer-card ans${i}" data-answer="${i}" ${answered?'disabled':''}><div class="letter">${String.fromCharCode(65+i)}</div><div>${esc(a)}</div></button>`).join('')}</div><div id="sent" class="player-sent ${answered?'':'hidden'}">${uiIcon('spark')}<span>Réponse envoyée. Attends la correction.</span></div><div class="player-footer-stats"><span>Score <b>${session.player?.score||0} pts</b></span><span>Série <b>${session.player?.streak||0}</b></span></div></main></div>`);document.querySelectorAll('[data-answer]').forEach(b=>b.onclick=()=>sendAnswer(Number(b.dataset.answer)));startTimer()}
function reveal(){clearInterval(timer);const q=session.question,mine=session.player?.answer,ok=mine===session.correctAnswer;shell(`<div class="player-live-shell"><header class="player-live-top"><div class="player-live-brand"><img src="./assets/icons/icon-64.png" alt=""><span><b>Correction</b><small>${esc(q.subject)} • ${esc(q.chapter)} • LEÇON ${esc(q.lesson)}</small></span></div><div class="player-live-chip">${session.player?.avatar?avatarMarkup(session.player.avatar,'avatar-svg avatar-game-svg',`Avatar de ${session.player?.name||''}`):''}<span>${session.player?.score||0} pts</span></div></header><main class="player-question-v7 player-correction-v7"><div class="player-result-banner ${ok?'success':'retry'}"><span>${ok?'✓':'↻'}</span><div><small>${ok?'BONNE RÉPONSE':'À RETENIR'}</small><h2>${ok?'Bien joué !':'Tu l’auras la prochaine fois.'}</h2></div></div><div class="player-question-index">QUESTION ${session.currentIndex+1}</div><h1>${esc(q.prompt)}</h1><div class="answer-grid player-answer-grid">${q.choices.map((a,i)=>`<div class="answer-card ans${i} ${i===session.correctAnswer?'correct':'dim'}"><div class="letter">${String.fromCharCode(65+i)}</div><div>${esc(a)}</div></div>`).join('')}</div><section class="player-explanation"><span>EXPLICATION</span><p>${esc(session.explanation||'')}</p></section><div class="player-footer-stats"><span>Ton score <b>${session.player?.score||0} pts</b></span><span>Attends la question suivante…</span></div></main></div>`) }
function ended(){clearInterval(timer);const me=(session.leaderboard||[]).find(x=>x.id===session.player?.id);shell(`<div class="player-live-shell"><header class="player-live-top"><div class="player-live-brand"><img src="./assets/icons/icon-64.png" alt=""><span><b>MCO Quiz Arena</b><small>SESSION TERMINÉE</small></span></div></header><main class="player-ended-v7"><div class="player-final-medal">${me?.rank===1?'1':me?.rank===2?'2':me?.rank===3?'3':'#'+(me?.rank||'—')}</div>${session.player?.avatar?`<div class="player-final-avatar">${avatarMarkup(session.player.avatar,'avatar-svg live-avatar-svg',`Avatar de ${session.player?.name||''}`)}</div>`:''}<span class="section-kicker">TON RÉSULTAT</span><h1>${me?`${esc(me.name)}, tu termines ${me.rank}${me.rank===1?'er':'e'}.`:'Partie terminée'}</h1><p>Score final : <b>${me?.score||0} points</b></p><section class="player-final-board"><div class="panel-title-row"><div><span class="section-kicker">CLASSEMENT</span><h3>Top 10</h3></div></div><div class="leaderboard">${(session.leaderboard||[]).slice(0,10).map(r=>`<div class="rank avatar-rank ${r.id===session.player?.id?'is-me':''}"><div class="place">${r.rank}</div>${r.avatar?`<span class="rank-avatar">${avatarMarkup(r.avatar,'avatar-svg avatar-rank-svg',`Avatar de ${r.name}`)}</span>`:''}<b>${esc(r.name)}</b><strong>${r.score}</strong></div>`).join('')}</div></section><div class="player-final-actions"><a class="btn primary" href="./join.html">Rejoindre un autre live</a><a class="btn soft" href="./student.html">Mon espace élève</a></div></main></div>`) }
function render(){if(session.status==='lobby')lobby();else if(session.status==='ended')ended();else if(session.revealed||session.status==='reveal')reveal();else question()}
async function state(){session=await rpc('mco_session_state',{p_code:code,p_teacher_key:null,p_player_id:auth.playerId,p_player_token:auth.playerToken});return session}
async function refresh(){try{await state();const key=`${session.status}-${session.currentIndex}-${session.revealed}-${session.player?.answer}-${session.player?.score}-${session.players.length}`;if(key!==last){last=key;render()}}catch(e){shell(`<div class="waiting premium-waiting"><div><h1>Connexion perdue</h1><p class="muted">${esc(e.message)}</p><a class="btn primary" href="./join.html">Revenir</a></div></div>`);clearInterval(poller)}}
async function sendAnswer(i){try{document.querySelectorAll('[data-answer]').forEach(b=>b.disabled=true);const elapsed=Math.max(0,Date.now()-questionShownAt);await rpc('mco_submit_answer',{p_code:code,p_player_id:auth.playerId,p_player_token:auth.playerToken,p_answer_index:i,p_elapsed_ms:elapsed});await channel?.broadcast('answer');await refresh()}catch(e){toast(e.message,'bad')}}
async function boot(){if(!isConfigured||!code||!auth){location.href='./join.html';return}try{await state();channel=liveChannel(code,refresh);poller=setInterval(refresh,1500);render()}catch{location.href='./join.html'}}
boot();
