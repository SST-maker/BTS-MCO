import { signInStudent, studentMe, isConfigured } from './supabase-client.js';
import { $, qs, toast, configWarning } from './common.js';
const root=$('#root');
function render(){root.innerHTML=`<div class="auth-shell student-auth-shell"><section class="auth-brand student-auth-brand"><div class="auth-brand-inner"><img src="./assets/branding/app-icon-source-officielle.png" class="auth-icon"><div class="eyebrow light">ESPACE ÉLÈVE • BTS MCO</div><h1>Apprends. Révise.<br><span>Progresse à ton rythme.</span></h1><p>Retrouve tes 156 leçons, les définitions clés, les méthodes, les exemples et des quiz ciblés ADOC / GO.</p><div class="auth-points"><span>📚 Leçons structurées</span><span>🧠 Quiz ciblés</span><span>⚡ Live classe</span></div></div></section><section class="auth-panel"><div class="auth-card"><a class="auth-back" href="./index.html">← Accueil</a><div class="mini-logo"><img src="./assets/icons/icon-96.png"><div><b>MCO Quiz Arena</b><small>Espace élève</small></div></div><h2>Connexion élève</h2><p class="muted">Utilise l’identifiant et le mot de passe donnés par ton professeur.</p>${!isConfigured?configWarning():''}<div class="field"><label>Identifiant</label><input id="identifier" autocomplete="username" placeholder="prenom.nom"></div><div class="field"><label>Mot de passe</label><div class="password-wrap"><input id="password" type="password" autocomplete="current-password" placeholder="••••••••"><button id="togglePwd" type="button">Afficher</button></div></div><button class="btn primary premium-cta" id="login">Entrer dans mon espace <span>→</span></button><div class="auth-security">🔒 Ton mot de passe n’est jamais enregistré en clair.</div><div class="auth-alt"><a href="./join.html">J’ai juste un code de live</a></div></div></section></div>`;wire()}
function wire(){const p=$('#password');$('#togglePwd').onclick=()=>{p.type=p.type==='password'?'text':'password';$('#togglePwd').textContent=p.type==='password'?'Afficher':'Masquer'};$('#login').onclick=login;p.onkeydown=e=>{if(e.key==='Enter')login()}}
async function login(){if(!isConfigured)return toast('Configure Supabase dans config.js','bad');const id=$('#identifier').value.trim(),pwd=$('#password').value;if(!id||!pwd)return toast('Identifiant et mot de passe requis','bad');try{$('#login').disabled=true;await signInStudent(id,pwd);const next=qs('next');location.href=next?decodeURIComponent(next):'./student.html'}catch(e){toast(e.message,'bad');$('#login').disabled=false}}
async function boot(){const s=await studentMe();if(s){const next=qs('next');location.href=next?decodeURIComponent(next):'./student.html';return}render()}
boot();
